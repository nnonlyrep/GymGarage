'use strict';

// Simple implementation of "get own property descriptor"
var $gOPD = Object.getOwnPropertyDescriptor;

if ($gOPD) {
  try {
    // Some old environments can throw here; this mirrors the real package's safety check
    $gOPD({}, 'test');
  } catch (e) {
    $gOPD = null;
  }
}

module.exports = $gOPD;
