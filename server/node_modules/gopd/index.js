'use strict';

// Minimal fallback implementation of "get own property descriptor"
var gOPD = Object.getOwnPropertyDescriptor;

if (gOPD) {
  try {
    // Some old environments throw when called like this; this check mirrors original package behavior
    gOPD({}, 'test');
  } catch (e) {
    gOPD = null;
  }
}

module.exports = gOPD;
